machine:
  post:
    - pyenv global 2.7.12 3.5.2
dependencies:
  post:
    - python2.7 setup.py install
    - python3.5 setup.py install
test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) python2.7 ;; 1) python3.5 ;; ./tests/test_instruction_set.py:
      parallel: true